#include <iostream>

int main(){
	std::cout << "++x - Prefix increments the value, and then proceeds with the expression. \n"
		  << "x++ - Postfix evaluates the expression and then performs the incrementing. \n";
	return 0;
}
